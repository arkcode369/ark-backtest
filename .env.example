# Telegram Bot Token dari @BotFather
TELEGRAM_TOKEN=your_telegram_bot_token_here

# AI Configuration (optional — defaults shown)
# AI_ENDPOINT=https://marketriskmonitor.com/api/analyze
# AI_MODEL=claude-opus-4-6

# Health check port (optional — default: 8080)
# HEALTH_PORT=8080

# Strategy file storage directory (optional — default: /storage)
# STORAGE_DIR=/storage
